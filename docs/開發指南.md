# CoolPC MCP Server 開發指南

**最後更新**: 2025-12-05 11:40 台北時區

---

## 📋 目錄

1. [環境設定](#環境設定)
2. [開發流程](#開發流程)
3. [常見開發任務](#常見開發任務)
4. [除錯與測試](#除錯與測試)
5. [部署指南](#部署指南)
6. [常見問題](#常見問題)
7. [貢獻指南](#貢獻指南)

---

## 環境設定

### 必要環境

#### 1. Python 環境

```bash
# 檢查 Python 版本 (需要 3.6+)
python3 --version

# 如果沒有安裝 Python
# macOS: 使用 Homebrew
brew install python3

# Linux (Ubuntu/Debian)
sudo apt-get install python3

# Windows
# 從 https://www.python.org/ 下載安裝
```

#### 2. Node.js 環境

```bash
# 檢查 Node.js 版本 (需要 18.0.0+)
node --version

# 如果沒有安裝 Node.js
# macOS: 使用 Homebrew
brew install node

# Linux: 使用 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18

# Windows
# 從 https://nodejs.org/ 下載安裝
```

### 專案初始化

```bash
# 1. Clone 專案
git clone https://github.com/shyuan/coolpc-mcp-server.git
cd coolpc-mcp-server

# 2. 安裝 Python 相依性
pip3 install -r requirements.txt

# 3. 安裝 Node.js 相依性
npm install

# 4. 下載最新產品資料
python3 coolpc_parser.py --download --json product.json

# 5. 建置 TypeScript
npm run build

# 6. 測試執行
npm run dev
```

### 開發工具建議

#### VS Code 擴充套件

```json
{
  "recommendations": [
    "esbenp.prettier-vscode",        // 程式碼格式化
    "ms-python.python",              // Python 支援
    "dbaeumer.vscode-eslint",        // TypeScript Linting
    "ms-vscode.vscode-typescript-next", // TypeScript
    "streetsidesoftware.code-spell-checker" // 拼字檢查
  ]
}
```

#### VS Code 設定

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "python.linting.enabled": true,
  "python.linting.pylintEnabled": true,
  "typescript.tsdk": "node_modules/typescript/lib"
}
```

---

## 開發流程

### 標準開發循環

```
1. 更新產品資料
   ↓
2. 修改程式碼
   ↓
3. 本地測試
   ↓
4. 建置與驗證
   ↓
5. 提交變更
   ↓
6. 推送到遠端
```

### 詳細步驟

#### 1. 更新產品資料

```bash
# 下載最新資料
python3 coolpc_parser.py --download --json product.json

# 查看解析摘要
python3 coolpc_parser.py product.json --summary

# 匯出 CSV (選用)
python3 coolpc_parser.py product.json --csv product.csv
```

#### 2. 開發 MCP Server

```bash
# 開發模式 (自動重載)
npm run dev

# 在另一個終端視窗測試
# (需要先設定 Claude Desktop)
```

#### 3. 建置檢查

```bash
# 建置 TypeScript
npm run build

# 檢查建置結果
ls -lh build/

# 執行建置版本
npm start
```

#### 4. Git 提交

```bash
# 查看變更
git status
git diff

# 加入變更
git add .

# 提交 (使用正體中文)
git commit -m "新增 XXX 功能"

# 推送
git push origin main
```

---

## Python 依賴管理

### requirements.txt 管理

專案使用 `requirements.txt` 管理 Python 套件依賴：

```bash
# 安裝所有依賴
pip3 install -r requirements.txt

# 新增套件（例如：pandas）
pip3 install pandas
pip3 freeze | grep pandas >> requirements.txt

# 更新所有套件到最新版本
pip3 install -r requirements.txt --upgrade

# 檢查已安裝的套件版本
pip3 list
```

### 虛擬環境（建議）

為避免套件版本衝突，建議使用 Python 虛擬環境：

```bash
# 建立虛擬環境
python3 -m venv venv

# 啟動虛擬環境
# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate

# 安裝依賴
pip3 install -r requirements.txt

# 離開虛擬環境
deactivate
```

---

## 常見開發任務

### 任務 1: 新增 MCP 查詢工具

#### 步驟

1. **在 `src/index.ts` 中定義工具**

```typescript
// 在 setupHandlers() 的 ListToolsRequestSchema 中新增
{
  name: "search_new_category",
  description: "搜尋新分類產品",
  inputSchema: {
    type: "object",
    properties: {
      parameter1: {
        type: "string",
        description: "參數說明"
      }
    }
  }
}
```

2. **實作查詢方法**

```typescript
private searchNewCategory(args: any) {
  const { parameter1 } = args;
  const results: any[] = [];

  // 查詢邏輯
  for (const cat of this.productData) {
    // 篩選邏輯
    if (/* 條件 */) {
      results.push(/* 產品 */);
    }
  }

  // 回傳結果
  return {
    content: [{
      type: "text",
      text: JSON.stringify({
        total_found: results.length,
        results: results
      }, null, 2)
    }]
  };
}
```

3. **註冊工具處理器**

```typescript
// 在 CallToolRequestSchema 的 switch 中新增
case "search_new_category":
  return this.searchNewCategory(args);
```

4. **測試新工具**

```bash
# 重新建置
npm run build

# 重啟 Claude Desktop
# 測試新工具
```

### 任務 2: 修改 HTML 解析邏輯

#### 情境：原價屋網站 HTML 結構變動

**步驟**:

1. **下載最新 HTML 檢查**

```bash
python3 coolpc_parser.py --download
```

2. **檢查 HTML 結構**

```python
# 在 coolpc_parser.py 中加入除錯輸出
def parse_html(self):
    with open(self.html_file, 'r', encoding='utf-8') as f:
        html_content = f.read()

    # 檢查 SELECT 標籤
    select_pattern = r'<SELECT[^>]*name=n(\d+)[^>]*>(.*?)</SELECT>'
    select_matches = re.findall(select_pattern, html_content, re.DOTALL | re.IGNORECASE)

    print(f"找到 {len(select_matches)} 個 SELECT 標籤")
```

3. **調整正則表達式**

```python
# 原本
option_pattern = r'<OPTION([^>]*?)>'

# 如果結構變動，可能需要調整
option_pattern = r'<OPTION\s+([^>]*?)>'
```

4. **測試解析**

```bash
# 測試解析
python3 coolpc_parser.py evaluate.html --summary

# 檢查輸出是否正確
python3 coolpc_parser.py evaluate.html --json test.json
python3 -m json.tool test.json > /dev/null && echo "JSON 格式正確"
```

### 任務 3: 優化品牌型號識別

#### 情境：某些產品的品牌型號提取不正確

**範例問題**:
- 輸入: "華碩 ASUS ROG STRIX B650E-F GAMING WIFI"
- 錯誤輸出: brand="華碩", model=None

**修正步驟**:

1. **找到問題產品**

```python
# 在 export_to_csv 後檢查
df = pd.read_csv('product.csv')
df[df['model'].isna()]  # 找出 model 為空的產品
```

2. **調整 `_extract_brand_model` 方法**

```python
def _extract_brand_model(self, text: str) -> Dict[str, str]:
    # 移除價格和特殊符號
    clean_text = re.sub(r'\$[0-9,]+.*$', '', text)
    clean_text = clean_text.strip()

    # 新增：處理華碩格式
    asus_match = re.match(r'^(華碩)\s+(ASUS)\s+([A-Za-z0-9\s\-]+?)(?:\s+GAMING|\s+WIFI|/)', clean_text)
    if asus_match:
        return {
            'brand': f"{asus_match.group(1)} {asus_match.group(2)}",
            'model': asus_match.group(3).strip()
        }

    # 其他邏輯...
```

3. **測試修正**

```bash
# 重新解析
python3 coolpc_parser.py evaluate.html --json product.json

# 檢查特定產品
python3 -c "
import json
with open('product.json') as f:
    data = json.load(f)
# 搜尋特定產品
"
```

### 任務 4: 新增產品分類映射

#### 情境：原價屋新增了新的產品分類 (例如 n31)

**步驟**:

1. **找出新分類 ID**

```bash
# 下載最新 HTML
python3 coolpc_parser.py --download

# 檢查 HTML 中的 SELECT 標籤
grep -o 'name=n[0-9]*' evaluate.html | sort -u
```

2. **更新 `_get_category_name` 方法**

```python
def _get_category_name(self, select_id: str) -> str:
    category_mapping = {
        '1': '品牌小主機、AIO|VR虛擬',
        # ... 現有映射 ...
        '30': '福利品出清',
        '31': '新分類名稱',  # 新增
    }

    return category_mapping.get(select_id, f"類別 {select_id}")
```

3. **重新解析並驗證**

```bash
python3 coolpc_parser.py evaluate.html --json product.json --summary
```

---

## 除錯與測試

### Python 解析器除錯

#### 1. 使用 Python 除錯器

```python
# 在需要除錯的地方加入
import pdb; pdb.set_trace()

# 執行時會在該處暫停
python3 coolpc_parser.py evaluate.html --json product.json
```

#### 2. 增加日誌輸出

```python
import logging

logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)

def parse_html(self):
    logger.debug(f"開始解析 {self.html_file}")
    # ...
```

#### 3. 驗證產品資料

```python
def validate_product(self, product):
    """驗證產品資料完整性"""
    issues = []

    if not product.get('brand'):
        issues.append('缺少品牌')

    if not product.get('price') or product['price'] <= 0:
        issues.append('價格無效')

    if not product.get('specs'):
        issues.append('缺少規格')

    if issues:
        print(f"產品 {product.get('index')} 問題: {', '.join(issues)}")
        print(f"  原始文字: {product.get('raw_text')}")
```

### TypeScript MCP Server 除錯

#### 1. 使用 console.error 輸出

```typescript
private searchCPU(args: any) {
  console.error('[DEBUG] searchCPU args:', JSON.stringify(args));

  const results = /* 查詢邏輯 */;

  console.error('[DEBUG] Found', results.length, 'results');

  return /* 結果 */;
}
```

**查看輸出**:
```bash
# 在開發模式下，console.error 會輸出到終端
npm run dev

# 在 Claude Desktop 中執行查詢，觀察終端輸出
```

#### 2. 檢查產品資料載入

```typescript
private loadProductData() {
  try {
    const productPath = join(__dirname, "../product.json");
    console.error('[INFO] Loading product data from:', productPath);

    const rawData = readFileSync(productPath, "utf-8");
    this.productData = JSON.parse(rawData);

    console.error('[INFO] Loaded', this.productData.length, 'categories');

    // 統計產品數量
    let totalProducts = 0;
    for (const cat of this.productData) {
      for (const subcat of cat.subcategories) {
        totalProducts += subcat.products.length;
      }
    }
    console.error('[INFO] Total products:', totalProducts);

  } catch (error) {
    console.error('[ERROR] Failed to load product data:', error);
  }
}
```

#### 3. 單元測試 (選用)

```typescript
// tests/search.test.ts
import { CoolPCMCPServer } from '../src/index';

describe('Product Search', () => {
  let server: CoolPCMCPServer;

  beforeEach(() => {
    server = new CoolPCMCPServer();
  });

  test('should search CPU by socket', () => {
    const results = server.searchCPU({ socket: 'AM5' });
    expect(results).toBeDefined();
  });
});
```

---

## 部署指南

### 本地開發部署

#### 1. 建置專案

```bash
# 清除舊的建置
rm -rf build/

# 建置 TypeScript
npm run build

# 驗證建置結果
node build/index.js
```

#### 2. 設定 Claude Desktop

**macOS**:

```bash
# 編輯設定檔
nano ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

**Windows**:

```bash
# 編輯設定檔
notepad %APPDATA%\Claude\claude_desktop_config.json
```

**設定內容**:

```json
{
  "mcpServers": {
    "coolpc": {
      "command": "node",
      "args": [
        "/完整路徑/coolpc-mcp-server/build/index.js"
      ],
      "env": {}
    }
  }
}
```

#### 3. 驗證部署

```bash
# 重新啟動 Claude Desktop

# 在 Claude Desktop 中測試
# 1. 檢查左下角是否顯示 🔌 圖示
# 2. 輸入測試查詢：「查詢 Intel CPU」
# 3. 觀察是否有回應
```

### 生產環境部署

#### 選項 A: Docker 容器化 (推薦)

```dockerfile
# Dockerfile
FROM node:18-alpine

WORKDIR /app

# 複製 package files
COPY package*.json ./

# 安裝相依性
RUN npm ci --only=production

# 複製程式碼
COPY . .

# 建置 TypeScript
RUN npm run build

# 執行
CMD ["node", "build/index.js"]
```

**建置與執行**:

```bash
# 建置 Docker Image
docker build -t coolpc-mcp-server .

# 執行容器
docker run -d --name coolpc-mcp coolpc-mcp-server
```

#### 選項 B: PM2 行程管理

```bash
# 安裝 PM2
npm install -g pm2

# 啟動服務
pm2 start build/index.js --name coolpc-mcp

# 查看狀態
pm2 status

# 查看日誌
pm2 logs coolpc-mcp

# 設定開機啟動
pm2 startup
pm2 save
```

---

## 常見問題

### Q1: 解析器無法下載 HTML

**錯誤訊息**:
```
網路錯誤: <urlopen error [Errno 8] nodename nor servname provided>
```

**解決方案**:
1. 檢查網路連線
2. 檢查防火牆設定
3. 使用代理伺服器（如果需要）

```python
# 在 coolpc_parser.py 中設定代理
import urllib.request

proxy_handler = urllib.request.ProxyHandler({
    'http': 'http://proxy.example.com:8080',
    'https': 'http://proxy.example.com:8080',
})
opener = urllib.request.build_opener(proxy_handler)
urllib.request.install_opener(opener)
```

### Q2: TypeScript 建置錯誤

**錯誤訊息**:
```
error TS2307: Cannot find module '@modelcontextprotocol/sdk'
```

**解決方案**:

```bash
# 刪除 node_modules 和 lock 檔案
rm -rf node_modules package-lock.json

# 重新安裝
npm install

# 重新建置
npm run build
```

### Q3: Claude Desktop 無法連接 MCP Server

**檢查清單**:

1. ✅ 確認 product.json 存在

```bash
ls -lh product.json
```

2. ✅ 確認建置成功

```bash
ls -lh build/index.js
```

3. ✅ 確認設定檔路徑正確

```bash
# macOS
cat ~/Library/Application\ Support/Claude/claude_desktop_config.json

# 檢查路徑是否存在
ls -lh /path/in/config/build/index.js
```

4. ✅ 查看 Claude Desktop 日誌

```bash
# macOS
tail -f ~/Library/Logs/Claude/mcp*.log
```

### Q4: 搜尋結果為空

**可能原因**:

1. **關鍵字拼寫錯誤**

```typescript
// 檢查是否使用小寫比對
searchText.toLowerCase().includes(keyword.toLowerCase())
```

2. **product.json 資料過舊**

```bash
# 更新產品資料
python3 coolpc_parser.py --download --json product.json

# 重新啟動 MCP Server
```

3. **分類名稱錯誤**

```bash
# 列出所有分類
python3 -c "
import json
with open('product.json') as f:
    data = json.load(f)
for cat in data:
    print(cat['category_name'])
"
```

---

## 貢獻指南

### 程式碼貢獻流程

1. **Fork 專案**

```bash
# 在 GitHub 上 Fork 專案
# Clone 你的 Fork
git clone https://github.com/your-username/coolpc-mcp-server.git
```

2. **建立功能分支**

```bash
git checkout -b feature/new-feature
```

3. **開發與測試**

```bash
# 開發新功能
# 執行測試
npm run build
npm run dev
```

4. **提交變更**

```bash
git add .
git commit -m "新增 XXX 功能"
```

5. **推送並建立 Pull Request**

```bash
git push origin feature/new-feature
# 在 GitHub 上建立 Pull Request
```

### 程式碼風格要求

- ✅ Python: 遵循 PEP 8
- ✅ TypeScript: 使用 2 空格縮排
- ✅ 註釋: 使用正體中文
- ✅ Commit message: 使用正體中文

### 測試要求

- ✅ 新功能需包含測試案例
- ✅ 確保現有測試通過
- ✅ 程式碼覆蓋率 > 80%

---

## 開發資源

### 官方文件

- **Model Context Protocol**: https://modelcontextprotocol.io/
- **TypeScript**: https://www.typescriptlang.org/
- **Python Regular Expressions**: https://docs.python.org/3/library/re.html

### 相關工具

- **JSON Viewer**: https://jsonviewer.stack.hu/
- **Regex Tester**: https://regex101.com/
- **TypeScript Playground**: https://www.typescriptlang.org/play

### 社群支援

- **GitHub Issues**: https://github.com/shyuan/coolpc-mcp-server/issues
- **Discord**: (待建立)

---

**文件版本**: 1.0
**撰寫日期**: 2025-12-05
**維護者**: Claude Code
